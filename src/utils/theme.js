import { colors } from "./colors";

export const theme = {
    primary: {
        color: colors.white,
        background: colors.primary_color
    },
    secondary: {
        color: colors.white,
        background: colors.secondary_color
    },
    ternary: {
        color: colors.white,
        background: colors.ternary_color
    }
}